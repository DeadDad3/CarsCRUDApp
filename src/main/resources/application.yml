spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/TransportCars
    username: postgres
    password: vezumu10
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        current_session_context_class: thread
